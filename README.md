# DNS-query-latency-checker
check your latency to any DNS server
python doh_test.py -h   

usage: doh_test.py [-h] [--doh-url DOH_URL] [--insecure] [--count COUNT]

options:
  -h, --help         show this help message and exit
  --doh-url DOH_URL
  --insecure         SSL ignore (must for IP)
  --count COUNT
